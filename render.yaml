# Render deployment configuration
# This file is used by Render.com for deployment settings

# Build command
build: npm install && npm run build

# Start command  
start: npm start

# Environment
node: 18

# Health check
healthCheckPath: /

# Auto-deploy
autoDeploy: true
